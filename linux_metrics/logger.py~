import logging
import sys
from . import integrated
def assemble(name,value,unit,tool,interval,isCumulative,Priority,Transform,Desception="NIL"):
    return "%s %f %s %s %d %s %f %s %s"
    
logger=logging.getLogger()
formatter=logging.Formatter('%(asctime)s %(message)s')
file_handler=logging.FileHandler("metric.log")
logger.addHandler(file_handler)
file_handler.setFormatter(formatter)
met=metric(1,'sda')
met.mem_stats
logger.warn(str(met.free_mem))
